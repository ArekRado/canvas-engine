export enum componentName {
  time = 'time',
  transform = 'transform',
  mouse = 'mouse',
  keyboard = 'keyboard',
  animation = 'animation',
  collider = 'collider',
  mouseInteraction = 'mouseInteraction',
  camera = 'camera',
  mesh = 'mesh',
  material = 'material',
}
