export enum componentName {
  time = 'time',
  transform = 'transform',
  mouse = 'mouse',
  keyboard = 'keyboard',
  animation = 'animation',
  collider = 'collider',
  rigidBody = 'rigidBody',
  mouseInteraction = 'mouseInteraction',
  camera = 'camera',
  mesh = 'mesh',
  material = 'material',
  line2D = 'line2D',
}
